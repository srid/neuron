document.addEventListener("DOMContentLoaded", function(event) {
    anchors.options = {
        placement: 'right',
        visible: 'always',
        class: 'anchor'
    };
    anchors.add('h2, h3, h4');
});
