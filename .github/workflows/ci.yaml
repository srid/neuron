name: "CI"
on:
  pull_request:
  push:
env:
  MAINLINE: refs/heads/nightly-releases-test
  BUNDLEFILE: neuron-bundle-linux-testing
jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest] # TODO , macos-latest]
    steps:
    - uses: actions/checkout@v2
    - uses: cachix/install-nix-action@v10
    # This also runs nix-build.
    - uses: cachix/cachix-action@v6
      with:
        name: srid
        signingKey: '${{ secrets.CACHIX_SIGNING_KEY }}'
        # Only needed for private caches
        authToken: '${{ secrets.CACHIX_AUTH_TOKEN }}'
    # This gets everything from cache, but doesn't build neuron (yet).
    - name: Pull from Nix cache
      run: nix-build -j4 shell.nix
    # This builds neuron, as well as run tests
    - name: Build neuron 
      run: nix-build -j4 ci.nix
    # macOS is not supported: https://github.com/matthewbauer/nix-bundle/issues/17
    # if: github.ref == 'refs/heads/master'
    - name: Bundle up
      if: ${{ github.ref == env.MAINLINE && runner.os == 'Linux' }}
      run: |
        git clone https://github.com/srid/nix-bundle.git tmp/nix-bundle
        cd tmp/nix-bundle
        ./nix-bundle.sh '(import ../.. {})' /bin/neuron > neuron
        mkdir ~/bundle
        cp neuron ~/bundle/neuron
        chmod a+x ~/bundle/neuron
    - name: Upload bundle as artifact 
      uses: actions/upload-artifact@v2
      if: ${{ github.ref == env.MAINLINE && runner.os == 'Linux' }}
      with:
        name: ${{ env.BUNDLEFILE }}
        path: ~/bundle/neuron
    - name: Get current date and time
      id: date
      run: echo "::set-output name=date::$(date +'%Y-%m-%d %H:%M:%S')"
    - name: Upload bundle to nightly release
      if: ${{ github.ref == 'refs/heads/nightly-releases-test' }}
      uses: meeDamian/github-release@v2.0.3
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        tag: nightly
        name: Neuron nightly
        body: >
          Current build of Neuron from `master` branch -
          last updated on ${{ steps.date.outputs.date }}.
        prerelease: true
        # TODO: Use BUNDLEFILE
        files: ~/bundle/neuron
        gzip: false
        allow_override: true
